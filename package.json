{
  "name": "p002-execution-engine",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Execution Engine do ecossistema TERTULIA.",
  "scripts": {
    "build": "tsc --noEmit",
    "test:execution": "tsx scripts/verify-execution.ts",
    "test": "npm run test:execution",
    "check": "npm run test && npm run build"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  }
}
